# Dependencies (installed inside the image)
node_modules

# Build output (rebuilt inside the image)
dist

# Version control
.git
.gitignore

# Tests and coverage
coverage
.nyc_output
# Keep the small, distributable provenance record because the production build
# copies it into dist; exclude only generation inputs and full-resolution art.
assets/artwork/source
assets/artwork/style
assets/artwork/prompt-plan.json
assets/artwork/prompt-overrides.json
assets/cards/qa

# Logs
*.log
logs

# Environment files
.env*

# Tooling and CI
.claude
.github
.vscode
.idea

# OS files
.DS_Store

# Docker files themselves
Dockerfile
docker-compose.yml
.dockerignore

# Misc
*.tgz
README.md
LICENSE
deploy.sh
